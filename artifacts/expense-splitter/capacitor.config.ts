import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuvaraj.spliteasy',
  appName: 'SplitEasy-Yuv',
  webDir: 'dist/public'
};

export default config;
